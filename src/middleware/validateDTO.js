// middleware koji provjerava jesu li podaci koje netko pošalje (npr. POST request) u ispravnom formatu prije nego što stignu do kontrolera

//export function validateDTO(schema) {
  //return (req, res, next) => {
    //const { error } = schema.validate(req.body);
    //if (error) {
    // return res.status(400).json({ message: error.details[0].message });
    //}
    //next();
  //};
//}
