// dodatak koji pomaže standardizirati kako se vraćaju odgovori tako da svaki res.json() ne izgleda drugačije

//export function success(res, data, message = "Uspjeh") {
//  return res.status(200).json({ success: true, message, data });
//}

//export function error(res, message = "Došlo je do pogreške", status = 500) {
//  return res.status(status).json({ success: false, message });
//}
